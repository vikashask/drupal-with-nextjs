# Drupal Backend Configuration
NEXT_PUBLIC_DRUPAL_BASE_URL=http://localhost:8080
DRUPAL_DOMAIN=localhost

# Basic Auth Credentials for JSON:API (development)
# These are used to authenticate API requests
DRUPAL_API_USERNAME=api_user
DRUPAL_API_PASSWORD=api_password

# Revalidation time for ISR (in seconds)
REVALIDATE_TIME=60

# Optional: Drupal OAuth credentials (for production)
DRUPAL_CLIENT_ID=
DRUPAL_CLIENT_SECRET=
